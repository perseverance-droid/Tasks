# network 学习笔记

## 1.网络模型
网络就是让不同设备互相传数据，分层是为了分工明确，每层干自己的事，出错也好排查。

## 2.TCP/IP
TCP：可靠传输，先建立连接再传数据，丢了会重发，保证数据不乱序。
三次握手：建立连接，确认双方收发都正常。
四次挥手：断开连接，确认双方数据都传完了。

## 3.HTTP
HTTP 是基于 TCP 的应用层协议，用来在网页和服务器之间传数据。
常见状态码：200（成功）、404（找不到）、500（服务器出错）。
请求/响应：客户端发请求，服务器处理后返回响应。

## 4.Wireshark
抓包工具，能看到网络里传输的数据包，用来分析通信过程。

 