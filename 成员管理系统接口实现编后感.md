# 成员管理系统接口实现编后感

在使用 Gin 开发成员管理系统接口时，我遇到了很多实际问题，也通过代码编写学到了很多知识。

一开始写接口时，我经常出现 404 错误，后来才知道是路由写错了。

```go
r.POST("/login", login)
```


通过规范路由写法，我终于能正常访问接口。

在接收前端数据时，参数绑定一直失败。

```go
var user Member
c.ShouldBindJSON(&user)
```

> 结构体标签要和 JSON 字段对应，才能正确接收数据。

最麻烦的是 密码加密后登录失败。

```go
bcrypt.GenerateFromPassword(...)
```


我反复调试后，终于理解了加密与验证逻辑，也懂得了密码不能存明文。

还有数据库操作经常出错，比如更新、删除语句不对。

```go
db.Model(&Member{}).Where("id=?", id).Update("status", 1)
```


通过练习，我现在能熟练完成增删改查。

这次实践让我掌握了 Gin 基本使用、接口开发流程、错误处理能力。
