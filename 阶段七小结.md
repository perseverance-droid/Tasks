# 阶段七小结

学习了 MySQL 关系型数据库的核心概念与 SQL 基础操作，掌握了增删改查（CRUD）的基本方法。同时学习了 Go 语言的 ORM 框架 Gorm，

遇到的主要问题与反思

## 1.数据库连接失败

- 错误：一开始直接复制 DSN 字符串，忘记把用户名、密码和数据库名改成自己的，导致一直连不上。

- 反思：连接数据库前一定要核对所有参数，尤其是 IP、端口、账号和密码，并且确认 MySQL 服务已经启动。

## 2.表不存在报错

- 错误：定义好结构体后直接执行 CRUD，忘记调用  AutoMigrate ，导致 Gorm 找不到对应的数据表。

- 反思：ORM 框架虽然方便，但表结构还是需要主动同步到数据库，不能想当然。

## 3.误操作全表数据

- 错误：在测试  Update  和  Delete  时，忘记加  WHERE  条件，差点把整张表的数据都改掉/删掉。

- 反思：写数据库操作时，必须养成“先写条件，再写操作”的习惯，生产环境中这是致命错误。

